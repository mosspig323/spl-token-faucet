{
  "name": "faucet",
  "version": "1.0.0",
  "scripts": {
    "test": "anchor test",
    "build": "anchor build",
    "predeploy:devnet": "npm run build",
    "deploy:devnet": "anchor deploy --url https://api.devnet.solana.com",
    "migrate:devnet": "anchor migrate --url https://api.devnet.solana.com"
  },
  "private": true,
  "dependencies": {
    "@project-serum/anchor": "^0.5.1-beta.2",
    "@project-serum/common": "0.0.1-beta.3",
    "@project-serum/serum": "^0.13.34",
    "@solana/spl-token": "^0.1.4"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.2"
  }
}
